#!/bin/bash

# Activate the virtual environment if necessary
source /home/{{ service_user }}/venv/bin/activate

# Run the Locust command
locust -f benchmarks/redis_get_set.py --headless -u 5 -r 5 --only-summary
